# FizzBuzz

Maven Java project (Java 21) — skeleton for a FizzBuzz implementation.

Structure:

- src/main/java/com/example/fizzbuzz/FizzBuzz.java  (main class)
- src/test/java/com/example/fizzbuzz/FizzBuzzTest.java (JUnit 5 tests)
- pom.xml (Maven build)

How to build and test locally:

```bash
# from project root
mvn -q -DskipTests=false test
```

Replace the placeholder test and implement `FizzBuzz` as required.
